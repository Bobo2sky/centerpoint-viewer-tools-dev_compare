# 评估指标详细说明

## NDS (nuScenes Detection Score) 相关指标

### 1. mAP (mean Average Precision)
- **含义**: 平均精度均值，衡量检测器的整体性能
- **计算方式**: 所有类别 AP 的算术平均值
- **取值范围**: [0, 1]，越大越好
- **当前值**: 0.4196

### 2. mATE (mean Average Translation Error)
- **含义**: 平均平移误差，衡量检测框中心位置的定位精度
- **计算方式**: 所有成功匹配的检测框与GT框中心点之间的欧氏距离的平均值
- **单位**: 米（m）
- **取值范围**: [0, ∞)，越小越好
- **当前值**: 0.1887 米

### 3. mASE (mean Average Scale Error)
- **含义**: 平均尺度误差，衡量检测框尺寸估计的准确性
- **计算方式**: 
  ```
  scale_error = (|pred_l - gt_l|/gt_l + |pred_w - gt_w|/gt_w + |pred_h - gt_h|/gt_h) / 3
  ```
  然后转换为 1-IOU 形式: `mASE = 1 - mean(scale_errors)`
- **取值范围**: [0, 1]，越大越好（因为是1-IOU形式）
- **当前值**: 0.9389（表示尺寸估计较准确）

### 4. mAOE (mean Average Orientation Error)
- **含义**: 平均方向误差，衡量检测框朝向角度的估计精度
- **计算方式**: 
  ```
  yaw_diff = |pred_yaw - gt_yaw|
  yaw_diff = min(yaw_diff, 2π - yaw_diff)  # 归一化到[0, π]
  mAOE = mean(yaw_diff)
  ```
- **单位**: 弧度（rad）
- **取值范围**: [0, π]，越小越好
- **当前值**: 0.2918 弧度 ≈ 16.72°

### 5. mAVE (mean Average Velocity Error)
- **含义**: 平均速度误差，衡量目标速度估计的准确性
- **计算方式**: 预测速度与真实速度的差值的平均值
- **单位**: m/s
- **取值范围**: [0, ∞)，越小越好
- **当前值**: 0.0000（当前数据集没有速度信息）

### 6. mAAE (mean Average Attribute Error) ⭐
- **含义**: 平均属性误差，衡量目标类别/属性分类的错误率
- **计算方式**: `mAAE = 1 - 分类准确率`
  - 分类准确率 = 正确分类的检测数 / 总检测数
  - 在当前实现中，检测框只与同类别的GT进行匹配
  - 因此所有成功匹配的检测框（TP）类别都是正确的
  - 分类准确率 = 100%
  - mAAE = 1 - 1.0 = 0.0
- **取值范围**: [0, 1]，越小越好
- **当前值**: 0.0000（表示类别分类100%正确）

#### mAAE 的特殊说明：

**为什么 mAAE = 0.0？**

在当前的评估实现中：
1. 我们将预测结果按类别分组
2. 对于每个类别，只在该类别的预测框和GT框之间进行匹配
3. 匹配成功的检测框（True Positive）天然就是类别正确的
4. 因此，所有 TP 的类别准确率为 100%，mAAE = 0.0

**如果要计算真实的类别错误率，应该怎么做？**

如果预测结果中可能存在类别错误（例如，把 Car 错误检测为 Bus），那么应该：
1. 不按类别分组，而是基于位置进行全局匹配
2. 对于每个GT框，找到距离最近且IoU最大的预测框（不限制类别）
3. 检查匹配的预测框类别是否与GT类别一致
4. 计算类别不一致的比例作为 mAAE

示例：
```
总匹配数: 100
类别正确: 95
类别错误: 5
准确率 = 95/100 = 0.95
mAAE = 1 - 0.95 = 0.05
```

**当前实现的合理性：**

当前实现假设预测结果的类别是可信的，重点评估：
- 定位精度（mATE）
- 尺寸精度（mASE）
- 方向精度（mAOE）
- 检测完整性（mAP、Precision、Recall）

这种实现方式适用于：
- 类别分类器已经很成熟的检测系统
- 评估重点在几何精度而非分类精度的场景

## NDS 计算公式

```
NDS = 1/10 * [5*mAP + (1-mATE) + mASE + (1-mAOE/π) + (1-mAVE) + (1-mAAE)]
```

各项说明：
- mAP 权重为 5，占 50%，是最重要的指标
- 其他5项各占 10%，共 50%
- 所有误差项都转换为"越大越好"的形式

**当前 NDS 值**: 0.6755

分解：
- 5 * 0.4196 (mAP) = 2.098
- 1 - 0.1887 (mATE) = 0.8113
- 0.9389 (mASE) = 0.9389
- 1 - 0.2918/π = 0.9072
- 1 - 0.0000 (mAVE) = 1.0000
- 1 - 0.0000 (mAAE) = 1.0000

总和 = 6.755 / 10 = 0.6755

## 总结

当前检测系统的表现：
- ✅ **类别分类**: 100% 准确（mAAE = 0.0）
- ✅ **定位精度**: 优秀，平均误差仅 18.87 cm
- ✅ **尺寸估计**: 优秀，mASE = 0.9389
- ✅ **方向估计**: 良好，平均误差 16.72°
- ⚠️ **整体检测能力**: 中等，mAP = 0.4196

改进方向：
1. 提高各类别的 AP，尤其是 barrier (0.24) 和 Cyclist (0.31)
2. 降低 False Positive 率，提高 Precision
3. 提高 Recall，减少漏检
